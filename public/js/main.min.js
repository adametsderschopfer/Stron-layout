(() => {
  const mobBtn = document.querySelector(".header_mobile_btn");
  const mob = document.querySelector(".header_mobile");
  let _switch = false;

  mobBtn.addEventListener("click", () => {
    mob.style.display = !_switch ? "flex" : "none"
    _switch = !_switch
  })

})();
